Legacy placeholder directory.

Parselton no longer uses bundled Sherpa, Kokoro, or Piper assets for text-to-speech.

The `TTS` folder stays in the project so the existing Xcode folder reference remains valid in development builds.
